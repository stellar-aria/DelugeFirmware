- [] remove TEST_VECTOR*
- [] sample accurate midi/usb note-on from embassy
- [] encapsulate deluge::dsp::fixed::Interpolator's tap windows (buffer_l/buffer_r are public because sample_low_level_reader / live-input play_head / wave_table manage them directly for loop crossfades + jump-fills — lift that behind a richer feed/window API so they can go private)
- [] deluge-stream boundary (future, unscoped): browser.cpp, sample_browser.cpp, and audio_file_manager.cpp's FilePointer usage (and the staticDIR/staticFNO externs, storage_manager.h:410-411) can't retire until this exists. Root cause (found while designing Tier 2, now merged — docs/superpowers/specs/2026-07-14-file-io-migration-tier2-locator-cache-design.md §6): AudioFileManager::buildAudioFileFromCard's SAMPLE branch (audio_file_manager.cpp:801-816) uses FilePointer.sclust to seed its own raw FAT cluster-chain walk (get_fat_from_fs/clst2sect), building a cluster→sector address table for ClusterByteSource's real-time SD streaming — a categorically different, irreducible capability than "open a file portably," which file_io.h/deluge::io shouldn't and can't absorb. docs/dev/target_architecture.md (lines 262-272) already names this split as intentional future work: deluge-files (file_io.h's territory, blocking-allowed) vs deluge-stream (audio-context storage: cluster cache, sample streaming, SD read scheduling — not yet built). The resource manager (crates/deluge_resource/) already has the right seam for this (Source.materialize, e.g. AudioFileManager::readClusterData) and needs no changes — it's already disk-address-oriented, not FAT-oriented; what's missing is the upstream one-time "resolve path → cluster/sector table" step, which today lives as raw FatFS-internals code directly in audio_file_manager.cpp. A deluge-stream boundary (something like deluge_stream_open/deluge_stream_read_at, sibling to file_io.h, feeding the resource manager's Source the same way readClusterData does today) is the real fix — a substantial, separate architecture project, not scoped by any existing design doc yet. **Write-side twin, found while implementing Tier 3 (docs/superpowers/specs/2026-07-14-file-io-migration-tier3-filereader-filewriter-design.md, "Correction: sample_recorder.cpp needed a raw-FatFS sibling of createFile"):** SampleRecorder::file (sample_recorder.h:145, real-time recording) needs the same category of raw FAT access on the write side — raw cluster addressing via file->inner().clust feeding clst2sect (sample_recorder.cpp:839), file->truncate()/file->lseek() and a raw FA_WRITE-only reopen for the final-size adjustment (sample_recorder.cpp:1529,1574,1578) — none of which deluge::io::File exposes, by the same design reasoning as the read side. `StorageManager::createFileRaw` (storage_manager.h/.cpp) is the current quarantine for this: a sibling of the portable createFile(), duplicating its ~35-line create-with-folder-retry logic against raw FatFS::File/FatFS::mkdir instead of deluge::io::File/deluge::io::mkdir. It is not a design mistake to fix locally — sample_recorder.cpp structurally cannot use deluge::io::File until a deluge-stream-style boundary gives it portable real-time cluster-level write access — but it IS real debt: retiring createFileRaw (and the FatFS::File it returns) is a real success criterion for whatever eventually implements this boundary, not a nice-to-have.
- [] smsysex.cpp manual smoke-test gate: Tier 4 of the file_io.h migration (docs/superpowers/specs/2026-07-14-file-io-migration-tier4-smsysex-design.md) is implemented and unit-tested, but has no automated wire-level test (none existed before this migration either). Before the next companion-app-facing release, do a manual pass — connect the companion app (or replay a raw SysEx dump) and exercise open/read/write/close/mkdir/rename/delete/getDirEntries/updateTime against a real card, confirming the wire's err/date/time/attr/size values are byte-identical to pre-migration behavior; also exercise a bad-filename case (open/mkdir/rename with an invalid path, collapsing FR_INVALID_NAME→FR_INVALID_PARAMETER) and a mid-operation card-pull case (collapsing FR_INVALID_OBJECT/FR_INT_ERR→FR_DISK_ERR), confirming the companion app tolerates these coarser `err` values same as the already-documented NOT_FOUND collapse.
