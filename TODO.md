- [] remove TEST_VECTOR*
- [] sample accurate midi/usb note-on from embassy
- [] encapsulate deluge::dsp::fixed::Interpolator's tap windows (buffer_l/buffer_r are public because sample_low_level_reader / live-input play_head / wave_table manage them directly for loop crossfades + jump-fills — lift that behind a richer feed/window API so they can go private)
- [] deluge::io migration Tier 2 (docs/superpowers/specs/2026-07-14-file-io-migration-roadmap-design.md §3): design a fast-handle equivalent for FatFS's FilePointer ({sclust, objsize} raw cluster+size), or redesign the browsing UI to not need it. This one design unblocks four files at once: browser.cpp's readFileItemsForFolder (where FilePointer originates and gets baked into every FileItem), sample_browser.cpp (consumes FileItem::filePointer as a fast-path into getAudioFileFromFilename), instrument_clip_view.cpp (shares the extern staticDIR/staticFNO globals with the above), and audio_file_manager.cpp (FilePointer's actual producer — resolveFilePointer/getAudioFileFromFilename/buildAudioFileFromCard, plus raw ld_clust/ld_dword FAT directory-entry parsing, a level deeper than any wrapper). file_io.h/deluge::io deliberately expose no cluster-level concept, so none of these four is a mechanical migration until this is designed.
- [] deluge::io migration Tier 3 (docs/superpowers/specs/2026-07-14-file-io-migration-roadmap-design.md §4): design giving storage_manager.cpp's FileReader/FileWriter a deluge::io::File member instead of a raw FIL (currently a base-class member, storage_manager.h:54-113) — every serializer/deserializer in the app (Serializer, XMLSerializer/Deserializer, JsonSerializer/Deserializer) inherits from these two classes, so every song/preset/XML/JSON load/save rides on this. The single largest, most architecturally central piece of the whole FatFS migration; also fix the readFIL.obj.id raw FFOBJID poke (storage_manager.cpp:292) while in there.
- [] smsysex.cpp manual smoke-test gate: Tier 4 of the file_io.h migration (docs/superpowers/specs/2026-07-14-file-io-migration-tier4-smsysex-design.md) is implemented and unit-tested, but has no automated wire-level test (none existed before this migration either). Before the next companion-app-facing release, do a manual pass — connect the companion app (or replay a raw SysEx dump) and exercise open/read/write/close/mkdir/rename/delete/getDirEntries/updateTime against a real card, confirming the wire's err/date/time/attr/size values are byte-identical to pre-migration behavior; also exercise a bad-filename case (open/mkdir/rename with an invalid path, collapsing FR_INVALID_NAME→FR_INVALID_PARAMETER) and a mid-operation card-pull case (collapsing FR_INVALID_OBJECT/FR_INT_ERR→FR_DISK_ERR), confirming the companion app tolerates these coarser `err` values same as the already-documented NOT_FOUND collapse.
