- [] remove TEST_VECTOR*
- [] sample accurate midi/usb note-on from embassy
- [] encapsulate deluge::dsp::fixed::Interpolator's tap windows (buffer_l/buffer_r are public because sample_low_level_reader / live-input play_head / wave_table manage them directly for loop crossfades + jump-fills — lift that behind a richer feed/window API so they can go private)
- [] smsysex.cpp manual smoke-test gate: Tier 4 of the file_io.h migration (docs/superpowers/specs/2026-07-14-file-io-migration-tier4-smsysex-design.md) is implemented and unit-tested, but has no automated wire-level test (none existed before this migration either). Before the next companion-app-facing release, do a manual pass — connect the companion app (or replay a raw SysEx dump) and exercise open/read/write/close/mkdir/rename/delete/getDirEntries/updateTime against a real card, confirming the wire's err/date/time/attr/size values are byte-identical to pre-migration behavior; also exercise a bad-filename case (open/mkdir/rename with an invalid path, collapsing FR_INVALID_NAME→FR_INVALID_PARAMETER) and a mid-operation card-pull case (collapsing FR_INVALID_OBJECT/FR_INT_ERR→FR_DISK_ERR), confirming the companion app tolerates these coarser `err` values same as the already-documented NOT_FOUND collapse.
- [] remove convert_cluster_data's `yield` callback + the AudioEngine pump lambda in Cluster::convertDataIfNecessary once AudioEngine::routine()/runRoutine() runs on the preemptive Embassy InterruptExecutor task — the cooperative mid-conversion yield is unnecessary under preemption (see src/deluge/storage/audio/stream/convert.h)
- [] FLOAT->Q31 conversion (`convert_range_simd` in src/deluge/storage/audio/stream/convert.h) is SIMD (`Argon<float>::ConvertTo<int32_t, 31>()`, i.e. `vcvtq_n_s32_f32`) only when compiling for real NEON/MVE (`__ARM_NEON` / `__ARM_FEATURE_MVE` -- the arm-none firmware, qemu-arm, Apple Silicon), gated by `#if` in `convert_range_simd`. On x86-SIMDe hosts (where the goldens render) it stays on the scalar `q31_from_float` path, because SIMDe's software emulation of `vcvtq_n_s32_f32` diverges from the scalar reference: input `1.0f` (exact saturation boundary) produced `0x80000000` instead of the saturated `0x7FFFFFFF`, and `NaN` produced `0x00000000` instead of saturating to `0x7FFFFFFF` (see convert_cluster_spec.cpp's FLOAT case, Phase 2d Task 4). The real-NEON SIMD path was verified bit-exact to the scalar reference under qemu-arm's instruction-accurate NEON emulation (~20,025 cases incl. saturation boundaries, denormals, NaN/inf, pseudo-random sweep, zero mismatches -- tests/qemu/spec/convert_simd_neon_parity_spec.cpp, Phase 2d Task 4b). Revisit the x86-SIMDe branch if/when SIMDe's `vcvtq_n_s32_f32` emulation is fixed upstream (could then drop the gate and go SIMD everywhere).
- [] the Rust BSP has NO non-probe debug transport: `deluge_log` (src/bsp/rust/src/services.rs:133) writes to `rtt_target::rprint!` only under the `rtt` cargo feature, and drops the string otherwise — without a debug probe attached the device is silent. The legacy RZA1 BSP had a fallback that exfiltrated debug text over the **MIDI DIN UART** (`bufferMIDIUart`, the `#else` of `HAVE_RTT` in src/bsp/rza1/drivers/uart/uart.c), corrupting the MIDI stream in exchange for output; that path retires with the legacy BSP. Two viable routes with opposite failure modes: (a) **blocking/polled TX on a spare SCIF channel** — rza1l-hal/src/uart.rs already drives all five SCIF channels and only SCIF0 (MIDI) and SCIF1 (PIC) are claimed, so SCIF2/3/4 are free at a real baud rate rather than 31250; works inside fault handlers, before the scheduler exists, and with interrupts off; gated on whether those pins are physically broken out on the PCB. (b) **USB-CDC** — embassy-usb 0.6 ships `class/cdc_acm.rs`, and src/bsp/rust/src/usb.rs already builds an embassy-usb device with the MIDI class (`EMBASSY_USB_MAX_INTERFACE_COUNT=8` leaves room); needs no probe or extra cable and has real throughput, but is dead during panics, hangs, early boot, and whenever USB itself is the broken thing. Likely end state is both, layered behind one `deluge_log` sink that routes by context. Note separately that any transport also needs `ENABLE_TEXT_OUTPUT`, defined for Debug/RelWithDebInfo only (src/deluge/CMakeLists.txt:61) — a Release image is silent regardless of transport.
- [] add a qemu-arm integration test for the SIMD FLOAT *wiring* (not just the primitive): the FLOAT store + prefix/tail loop plumbing in `convert_range_simd` (convert.h) currently runs on NO automated test — the x86 spec exercises that plumbing only on the scalar path (FLOAT is gated off on x86), and the qemu parity spec (convert_simd_neon_parity_spec.cpp) tests `Argon<float>::ConvertTo` in isolation, not through `convert_range_simd`/`convert_cluster_data`. Cover the FLOAT integration path on real NEON (Phase 2d final-review union gap; consider making the qemu-arm convert parity a standing dual-arch gate).
