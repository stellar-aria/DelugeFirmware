# Lens 2 (Task 9) TSan classifier: KNOWN, pre-existing, BROAD debt patterns.
#
# One extended-regex PATTERN per line (matched with `grep -E` against a normalized TSan
# `SUMMARY:` line — see run.sh's `normalize()`). Blank lines and `#` comments are ignored.
# Order matters: run.sh checks `open_findings_patterns.txt` FIRST, so a line matching both
# (e.g. `loader.cpp:82` below, which is inside a file this list's broad `loader\.cpp`-style
# reasoning would otherwise also catch) is correctly bucketed as an open finding, not here.
#
# WHY PATTERNS, NOT EXACT LINES: an early version of this catalog used exact `SUMMARY:` line
# matches (Spike B's style). Running the harness fresh a few more times immediately produced
# a LONG tail of new exact lines — different song-load/UI/Arpeggiator/reverb fields lit up
# by run-to-run scheduling variance — all belonging to the SAME already-understood debt
# class: nearly the WHOLE app's main-thread/UI/song-model/reverb/playback-transport state was
# written assuming cooperative (non-preemptive) scheduling, and none of it was made
# thread-safe when the audio task moved onto its own real preemptive OS-thread/interrupt
# executor (a change that predates this task — see the embassy-scheduler-migration and
# audio-callback-relocation work). Exact-line matching against that class is an unwinnable,
# ever-growing whack-a-mole; a broad FILE/SUBSYSTEM pattern is the honest, maintainable
# classification — it says "yes, this is that same well-understood, out-of-scope debt",
# without pretending to have enumerated every specific field it can touch.
#
# What stays OUT of this file (by design, so it is never accidentally swallowed into "known,
# ignore"): anything on the actual storage/cluster/recorder/resource-manager data path this
# task exists to check — see open_findings_patterns.txt for that (checked first).

# AudioEngine's own cross-thread bookkeeping/render/reverb-param globals.
src/deluge/processing/engines/audio_engine\.cpp
# The reverb DSP internals (freeverb Comb/Allpass/Freeverb, Base/Reverb) — audio-thread
# render vs main-thread reverb-macro param update.
src/deluge/dsp/(\.\./dsp/)?reverb/
# UI root pointer + UI timer manager (main-thread UI state read from the audio-thread idle
# callback path).
src/deluge/gui/ui/ui\.cpp
src/deluge/gui/ui_timer_manager\.cpp
# LoadSongUI's own progress-flag reads/song-swap orchestration (as opposed to the specific
# storage-cluster interactions catalogued in open_findings_patterns.txt).
src/deluge/gui/ui/load/load_song_ui\.cpp
# Transport / playback-mode state during setup or a song swap.
src/deluge/playback/playback_handler\.cpp
src/deluge/playback/mode/arrangement\.cpp
# Song-model object-graph mutation during a live song swap while the OLD song's audio graph
# is still rendering on the audio thread (a related but DIFFERENT — and much broader —
# "song swap isn't preemption-safe" architectural gap than the narrower streaming/cluster
# property this task targets; see task-9-report.md's "related but out-of-scope" note).
src/deluge/deluge\.cpp
src/deluge/modulation/arpeggiator\.cpp
src/deluge/model/instrument/kit\.cpp
src/deluge/model/output\.cpp
# Debug-only allocator bookkeeping (MEM_GUARD's live-alloc-count tracking) and the host-only
# shared C stdio scratch buffer (two threads' D_PRINTLN sharing one vsnprintf buffer — a host
# logging artifact, not firmware behaviour).
src/deluge/memory/alloc_metadata\.cpp
src/lib/printf\.c
# loader.cpp's ONE admission-guard read of AudioEngine::audioRoutineLocked (already covered,
# as the WRITE side, by the audio_engine.cpp pattern above) — deliberately NOT covered by a
# broad `storage/audio/stream/` pattern (that would also swallow the genuinely-new recorder/
# cluster findings in open_findings_patterns.txt), so this one line gets its own entry.
src/deluge/storage/audio/stream/loader\.cpp:82:32 in deluge::audio::stream::loader::pump

# --- Surfaced during R1 (efatfs read migration) Lens 2, 2026-07-22 ---
# Full-stack-attributed to PRE-EXISTING classes; 0/18 touch R1's efatfs read
# surface (no efatfs/read_cluster_data/EfatfsReadSource/block_on_fiber-from-read
# /ProdOps::read/sector_of-FAT-walk frame in either racing stack). Classes are
# documented in docs/dev/known-concurrency-bugs.md (TLSF allocator; playback/
# song-transport tick-vs-restart; AudioEngine global flags). Cataloged per
# decision 2026-07-22 — recorded, not suppressed.
#
# Specificity trade-off: unlike this file's broad file-level patterns above
# (audio_engine.cpp, reverb/, etc.), these entries pin file+line+function (the
# loader.cpp:82 style, not a bare wildcard) — deliberately, per the R1 review
# instruction not to risk masking a genuinely-new future race on these same,
# frequently-touched files (clip.cpp, song.cpp, playback_handler.h). A future
# race at a DIFFERENT line in these files will still correctly surface as
# UNCATALOGUED, unlike the broad-file convention used elsewhere in this catalog.

# TLSF allocator (crates/deluge_alloc/src/tlsf.rs): audio-thread voice
# allocation racing recorder cluster-table growth in the shared heap. Ten
# specific known sites pinned by line number, plus the two generic
# core::ptr::read/write<*mut BlockHeader> monomorphizations they lower to.
crates/deluge_alloc/src/tlsf\.rs:(269|276|277|314|317|348|68|92):[0-9]+ in <deluge_alloc::tlsf::(Tlsf|BlockHeader)>
core::ptr::(read|write)::<\*mut deluge_alloc::tlsf::BlockHeader>

# Playback/song-transport: audio-thread tick (processCurrentPos / clock-active
# check) vs. (re)setupPlayback writes on the same Clip/Song/NoteRow/
# PlaybackHandler state. Same "cooperative-only" class as the broader
# playback_handler.cpp/arrangement.cpp patterns above, but pinned per-site here
# (see specificity trade-off note above).
src/deluge/model/clip/clip\.cpp:223:6 in Clip::processCurrentPos
src/deluge/model/clip/clip\.cpp:245:6 in Clip::processCurrentPos
src/deluge/model/clip/instrument_clip\.cpp:704:6 in InstrumentClip::processCurrentPos
src/deluge/model/note/note_row\.cpp:2137:29 in NoteRow::processCurrentPos
src/deluge/model/song/song\.cpp:4002:15 in Song::isClipActive
src/deluge/playback/playback_handler\.h:223:46 in PlaybackHandler::isEitherClockActive

# AudioEngine global flags: audio-thread vs. UI/main-thread reads/writes of
# module-level bool flags, same class as the audio_engine.cpp pattern above
# but in two different files (pinned per-site, not file-wide).
src/deluge/gui/ui/audio_recorder\.cpp:154:29 in AudioRecorder::beginOutputRecording
src/deluge/gui/waveform/waveform_renderer\.cpp:69[14]:33 in WaveformRenderer::advanceOverviewScan

# --- Surfaced during R4 Phase C (delete C-FatFS) Task 8 gate, 2026-08-06 ---
# 5 runs x 500 blocks, cordae fixture. Full-stack-attributed to PRE-EXISTING
# classes; 0/4 classes (29 distinct summary lines) touch R4's storage/cluster/
# loader/recorder/mount surface (no storage/, cluster, loader.cpp beyond the
# already-pinned line 58, recorder, mount, or efatfs frame in either racing
# stack of any instance). Cataloged per decision 2026-08-06 -- recorded, not
# suppressed. Four classes:
#
#   1. TLSF allocator (see the pinned line-list below) -- two more exact sites
#      (Tlsf::check at 446, BlockHeader::is_prev_free at 80) than the R1 list,
#      same heap-walk-vs-allocation race, verified via full-stack backtrace
#      (GeneralMemoryAllocator::checkEverythingOk -> AudioEngine::routine()).
#
#   2. The debug-log shared C stdio scratch buffer (src/lib/printf.c pattern,
#      line 53 above) racing itself between logDebug/Debug::println call
#      sites on two threads. Verified via full-stack backtrace on every
#      instance this run (grep the run logs' "#1" frames): always
#      logDebug/print.cpp/log.cpp <-> printf.c's _out_rev/_vsnprintf/_ftoa.
#      The NEW wrinkle: on THIS run TSan chose the OTHER thread's frame (the
#      generic runtime interceptor itself, which carries no file/line) as the
#      SUMMARY site, so the existing printf.c pattern -- which matches the
#      file-bearing side -- doesn't catch it. Matched here by interceptor
#      name/Rust-str-validation-site instead:
core::ffi::c_str::strlen::runtime
 in strlen$
 in __tsan_memcpy$
library/core/src/str/validations\.rs:23[23]:52 in core::str::validations::run_utf8_validation
#      CAVEAT for a future reviewer: these three patterns key off generic
#      runtime-intercepted function names that carry NO file/subsystem info in
#      the SUMMARY line itself (run.sh only classifies the one-line SUMMARY,
#      not the full stack) -- every instance observed on 2026-08-06 was
#      verified (via full-stack backtrace) to be the printf.c log-buffer
#      class, but a hypothetical FUTURE genuinely-new strlen/memcpy race
#      elsewhere (including on a storage path) would also match. If this
#      pattern ever needs to earn back specificity, the fix is a run.sh change
#      to classify on the full stack, not just the SUMMARY line.
#
#   3. Song-model object-graph CONSTRUCTION/TEARDOWN during a live song swap
#      (LoadSongUI::performLoad() building a brand-new Song's full DSP/param
#      object graph -- ParamManager, GlobalEffectable(ForSong), Sound,
#      SoundInstrument, ModControllableAudio, ModFXProcessor, AutoParam, and
#      their owned compressor/delay/filter DSP components -- while the OLD
#      song's audio graph is still rendering on the audio thread) racing the
#      audio thread's read of the corresponding old-song state, PLUS the
#      backing heap zero-fill (deluge::memory::heaps.cpp's alloc_fast/
#      finalize) for that construction. Verified via full-stack backtrace:
#      every instance's other-thread frame lands in GlobalEffectable::
#      createDelayWorkingState/setupFilterSetConfig, Delay::isActive,
#      FilterSet::reset, or ParamManager::destructAndForgetParamCollections --
#      all reachable only from LoadSongUI::performLoad()'s Song construction,
#      the SAME architectural gap already covered above by the deluge.cpp/
#      arpeggiator.cpp/kit.cpp/output.cpp file-wide patterns, just touching
#      deeper per-Song owned-object constructors this run (new exact leaf
#      sites, not a new class):
src/deluge/model/song/song\.(cpp|h)
src/deluge/model/global_effectable/
src/deluge/model/mod_controllable/
src/deluge/modulation/params/param_manager\.cpp
src/deluge/modulation/automation/auto_param\.cpp
src/deluge/processing/sound/
src/deluge/dsp/compressor/
src/deluge/dsp/delay/
src/deluge/dsp/(\.\./dsp/)?filter/
src/deluge/memory/heaps\.cpp
#
#   4. WaveformRenderer::advanceOverviewScan -- same AudioRecorder/
#      WaveformRenderer flag-read class as the pinned entry above, one line
#      earlier (691 vs. 694) this run; folded into that entry's column-4
#      character class above rather than a separate line.
