# Lens 2 TSan open findings: REAL races on the cluster/loaded/resource-manager/scheduler
# state this harness checks — found by this harness, NOT suppressed, NOT fixed here. These
# fire under sustained playback + concurrent recording once the audio task is genuinely
# preemptive relative to the loader/fiber thread (the Embassy InterruptExecutor target;
# the cooperative BSP is unaffected). run.sh reports matches here as "OPEN FINDING (real,
# not suppressed)" — kept visible in every run's summary, distinct from the broad
# pre-existing baseline in known_patterns.txt.
#
# One extended-regex PATTERN per line (grep -E). IMPORTANT: run.sh matches each race by its
# normalized TSan SUMMARY LINE — the top-frame "... in <function>" — NOT the full stack. So a
# pattern must name the symbol that appears as the race's TOP frame, not a deeper caller.
#
# ── STATUS (2026-07-19): the SampleRecorder/SampleStream recorder-hand-off races formerly
#    listed here are FIXED — see docs/dev/known-concurrency-bugs.md (B2 table_ realloc UAF;
#    B3 index/payload hand-off). Verified GONE by this harness: no SUMMARY race on
#    SampleRecorder::createNextCluster / cardRoutine / writeCluster or SampleStream::chunk_at /
#    entry, and no recorder-payload torn write. Those patterns are retired. The recorder's
#    createNextCluster now only participates in B1, via get_cluster -> Manager::request.

# --- [B1] deluge_resource::manager::Manager's Cell<ChunkSlot> chunk table (HIGH, STILL OPEN).
#     Manager::release (audio thread, via Voice/SampleLowLevelReader unassign ->
#     cluster::remove_reason -> release_lease) races Manager::loader_next / request / acquire /
#     evict_lowest (loader-fiber OR audio thread, picking/leasing the next cluster) on the SAME
#     Cell<ChunkSlot> slot. Manager was written single-threaded (see its own stat() doc) but is
#     reached from Rust-unsafe extern "C" entry points with no lock, so nothing enforces that
#     assumption once C++ calls it from two real OS threads. A non-atomic lease-count RMW race
#     can silently lose an update -> premature eviction of a still-playing cluster, or a queued
#     flag never cleared -> the loader spins on a phantom queue entry. Reached from BOTH
#     playback (Voice::render -> SampleLowLevelReader::moveOnToNextCluster -> SampleStream::
#     get_cluster -> deluge_resource_request -> Manager::request) AND — now that B2/B3 are
#     fixed — the recorder (SampleRecorder::createNextCluster -> get_cluster -> Manager::request).
Cell<deluge_resource::manager::ChunkSlot>
core::mem::replace::<deluge_resource::manager::ChunkSlot>
# B1 also surfaces as a bare compiler-helper SUMMARY with no source frame, when the ChunkSlot
# move is lowered to a memory intrinsic (observed: `in __tsan_memset`, whose stack is
# core::mem::replace::<ChunkSlot> -> Cell::set -> Manager::request). B3's recorder-payload
# memmove/write is FIXED, so a bare memmove/memset/memcpy SUMMARY now traces to B1's Manager,
# NOT the recorder — re-verify the paired stack before treating a future match as anything else.
in __tsan_memmove$
in __tsan_memset$
in __tsan_memcpy$

# --- [SCHED] rung-5 two-level priority queue, async-SD staging ladder (STILL OPEN, out of
#     scope for the recorder fix). deluge_rust::fiber::enqueue writes deluge_rust::fiber::Q_COUNT
#     (and the priority ring links) on the AUDIO thread — via
#     loader::request_pump -> Coalescer::request -> deluge::storage::Owner::run_priority ->
#     deluge_worker_run_priority -> fiber::enqueue — while the fiber (owner) thread reads
#     Q_COUNT in deluge_rust::fiber::queue_nonempty / dequeue. The rung-5 priority-queue
#     counters have no synchronization between the audio-thread HIGH-priority enqueue and the
#     fiber's queue check. NOT introduced by the recorder-hand-off fix (which touched none of
#     fiber.rs / owner.cpp / loader.cpp) — a pre-existing async-SD scheduler race, surfaced
#     here under preemptive audio + sustained streaming. Same class as B1 (single-threaded-by-
#     assumption structure reached from two OS threads).
in deluge_rust::fiber::enqueue$
in deluge_rust::fiber::queue_nonempty$
in deluge_rust::fiber::dequeue$
